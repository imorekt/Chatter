{
  "name": "chatapp-root",
  "version": "1.0.0",
  "description": "Root package for deployment",
  "scripts": {
    "start": "cd backend && npm install && node index.js"
  },
  "engines": {
    "node": ">=18.x"
  }
}
